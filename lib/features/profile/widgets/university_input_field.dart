import 'package:flutter/material.dart';

class UniversityInputField extends StatelessWidget {
  const UniversityInputField({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}